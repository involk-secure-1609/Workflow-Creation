name: Generation of Emoji on Merging

on:
  pull_request:
    types:
    - closed

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Generation of emoji on closing of pull request
        run: echo "🎉 😁 😅 😅 😅 🎉"
